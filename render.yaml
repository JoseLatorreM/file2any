services:
  - type: web
    name: file2any-api
    runtime: docker
    region: oregon
    plan: free
    rootDir: server
    dockerfilePath: ./Dockerfile
    envVars:
      - key: NODE_ENV
        value: production
      - key: FRONTEND_URL
        value: https://files2any.com
    healthCheckPath: /health
